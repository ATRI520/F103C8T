; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

; CubeMX places application sources under Core/Src (PlatformIO defaults to src/).
[platformio]
src_dir = Core/Src
include_dir = Core/Inc

[env:genericSTM32F103C8]
platform = ststm32
board = genericSTM32F103C8
framework = stm32cube
upload_protocol = stlink
; Avoid bluepill's reset_config none (can break SWD on some ST-Link clones).
; Strip OpenOCD's post-flash hardware reset when NRST is not connected (see scripts/pio_openocd_upload.py).
extra_scripts = post:scripts/pio_openocd_upload.py